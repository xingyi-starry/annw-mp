name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 30
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: '1'
      DOTNET_NOLOGO: '1'

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Validate tag and game references
        id: version
        shell: pwsh
        run: |
          $source = Get-Content -Raw -LiteralPath '.\src\XingyiStarry.Mp\XingyiStarryMpPlugin.cs'
          $match = [regex]::Match($source, 'PluginVersion\s*=\s*"([^"]+)"')
          if (-not $match.Success) { throw 'Could not determine PluginVersion.' }
          $version = $match.Groups[1].Value
          if ($env:GITHUB_REF_NAME -ne "v$version") {
            throw "Tag $env:GITHUB_REF_NAME does not match PluginVersion $version."
          }
          $gameRoot = '${{ vars.ANNW_GAME_ROOT }}'
          if ([string]::IsNullOrWhiteSpace($gameRoot)) { $gameRoot = $env:ANNW_GAME_ROOT }
          if ([string]::IsNullOrWhiteSpace($gameRoot)) {
            throw 'Configure ANNW_GAME_ROOT as a repository variable or runner environment variable.'
          }
          if (-not (Test-Path -LiteralPath (Join-Path $gameRoot 'AnnW_Data\Managed\Assembly-CSharp.dll'))) {
            throw "ANNW_GAME_ROOT is not a valid game installation: $gameRoot"
          }
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "game_root=$gameRoot" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Build both release archives
        shell: pwsh
        run: |
          .\package.ps1 -Configuration Release `
            -GameRoot '${{ steps.version.outputs.game_root }}' `
            -ArtifactsPath (Join-Path $PWD '.artifacts\release') `
            -OutputDirectory (Join-Path $PWD '.release-output')

      - name: Run protocol tests
        shell: pwsh
        run: |
          dotnet '.\.artifacts\release\bin\XingyiStarry.Mp.Tests\release\XingyiStarry.Mp.Tests.dll'

      - name: Audit archives and write checksums
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.version }}'
          $standard = ".\.release-output\XingyiStarry-MP-$version.zip"
          $bundled = ".\.release-output\XingyiStarry-MP-$version-with-BepInEx.zip"
          foreach ($archive in @($standard, $bundled)) {
            if (-not (Test-Path -LiteralPath $archive)) { throw "Missing archive: $archive" }
          }
          $standardEntries = tar -tf $standard
          if ($standardEntries -match '(^|/)BepInEx/core/') { throw 'Standard archive contains BepInEx core.' }
          $bundledEntries = tar -tf $bundled
          if ($bundledEntries -notmatch 'BepInEx/core/BepInEx.dll') { throw 'Bundled archive is missing BepInEx.' }
          foreach ($required in @(
            'Uninstall-XingyiStarry-MP.bat',
            'Uninstall-XingyiStarry-MP.ps1',
            'XingyiStarry.Mp-LICENSE.txt',
            'XingyiStarry.Mp-THIRD-PARTY-NOTICES.md',
            'XingyiStarry.Mp-Licenses/Google.Protobuf-3.25.3-BSD-3-Clause.txt'
          )) {
            if ($required -notin $standardEntries) { throw "Standard archive is missing $required." }
          }
          $forbidden = 'EarlyPatcher|DebugTools|NoSteam|AnnW\.LanMp'
          if (($standardEntries + $bundledEntries) -match $forbidden) { throw 'A release archive contains development-only files.' }
          Get-FileHash -Algorithm SHA256 -LiteralPath $standard, $bundled |
            ForEach-Object { "$($_.Hash.ToLowerInvariant())  $(Split-Path -Leaf $_.Path)" } |
            Set-Content -LiteralPath '.\.release-output\SHA256SUMS.txt' -Encoding ASCII

      - name: Publish GitHub release
        uses: actions/github-script@v7
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const version = process.env.RELEASE_VERSION;
            const tag = context.ref.replace('refs/tags/', '');
            let release;
            try {
              release = (await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag
              })).data;
            } catch (error) {
              if (error.status !== 404) throw error;
              release = (await github.rest.repos.createRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag_name: tag,
                name: `XingyiStarry MP ${version}`,
                generate_release_notes: true,
                draft: false,
                prerelease: false
              })).data;
            }
            const files = [
              `XingyiStarry-MP-${version}.zip`,
              `XingyiStarry-MP-${version}-with-BepInEx.zip`,
              'SHA256SUMS.txt'
            ];
            for (const name of files) {
              const file = path.join(process.cwd(), '.release-output', name);
              const existing = release.assets.find(asset => asset.name === name);
              if (existing) {
                await github.rest.repos.deleteReleaseAsset({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  asset_id: existing.id
                });
              }
              await github.rest.repos.uploadReleaseAsset({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: release.id,
                name,
                data: fs.readFileSync(file),
                headers: {
                  'content-type': name.endsWith('.zip') ? 'application/zip' : 'text/plain',
                  'content-length': fs.statSync(file).size
                }
              });
            }
